import { CakeFilterPipe } from './cake-filter.pipe';

describe('CakeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CakeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
