import { NavBarShowHideDirective } from './nav-bar-show-hide.directive';

describe('NavBarShowHideDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBarShowHideDirective();
    expect(directive).toBeTruthy();
  });
});
